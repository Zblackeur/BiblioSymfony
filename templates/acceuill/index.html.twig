{% extends 'base.html.twig' %}

{% block title %}AcceuillController!{% endblock %}

{% block body %}
 <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bienvenue dans Notre Nouvelle Bibliotheque</h1>
    <section class="article">
        {% for article in listes %}
            <article>

                <div class="content">
                    <img src="http://placehold.it/350x150" alt="">
                    <h2>{{ article.titre }}</h2>
                    <div class="metadata">date Edition: {{ article.dateedition | date('d/m/Y') }}</div>

                    <a href="{{ path('livrespe',{'id':article.id}) }}" class="btn btn-primary">lire la suite</a>
                </div>
            </article>

        {% endfor %}


    </section>


    {#
  <h1>MediaTek Z</h1>

Menu
<ul>
    <li><a href="{{path('livre_index')}}">Livre</a></li>
    <li><a href="{{path('auteur_index')}}">Auteur</a></li>
    <li><a href="{{path('editeur_index')}}">Editeur</a></li>
    <li><a href="{{path('categorie_index')}}">Categorie</a></li>
</ul>
  #}
</div>
{% endblock %}
